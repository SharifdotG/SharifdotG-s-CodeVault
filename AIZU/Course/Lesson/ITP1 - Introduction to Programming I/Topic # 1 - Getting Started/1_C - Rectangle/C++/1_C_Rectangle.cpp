#include <bits/stdc++.h>

using namespace std;

int main() {
    int length, breadth;
    cin >> length >> breadth;

    cout << length * breadth << " " << 2 * (length + breadth) << endl;
    
    return 0;
}