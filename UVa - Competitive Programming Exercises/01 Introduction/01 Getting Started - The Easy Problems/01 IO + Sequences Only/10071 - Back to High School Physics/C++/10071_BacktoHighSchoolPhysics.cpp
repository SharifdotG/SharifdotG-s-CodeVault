#include <bits/stdc++.h>

using namespace std;

int main() {
    int velocity, time;

    while (cin >> velocity >> time) {
        cout << 2 * velocity * time << endl;
    }
    
    return 0;
}