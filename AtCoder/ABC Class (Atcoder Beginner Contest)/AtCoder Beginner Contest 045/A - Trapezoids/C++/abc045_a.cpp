#include <bits/stdc++.h>

using namespace std;

int main() {
    int upperBase, lowerBase, height;
    cin >> upperBase >> lowerBase >> height;

    cout << (upperBase + lowerBase) * height / 2 << endl;
    
    return 0;
}