#include <bits/stdc++.h>

using namespace std;

int main() {
    int testCases;
    cin >> testCases;

    while (testCases--) {
        int pages, wordsPerPage;
        cin >> pages >> wordsPerPage;

        cout << pages * wordsPerPage << endl;
    }
    
    return 0;
}