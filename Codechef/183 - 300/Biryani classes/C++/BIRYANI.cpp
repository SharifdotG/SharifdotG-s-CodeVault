#include <bits/stdc++.h>

using namespace std;

int main() {
    int testCases, weeks, coins;
    cin >> testCases;

    while (testCases--) {
        cin >> weeks >> coins;
        cout << weeks * coins << endl;
    }
    
    return 0;
}