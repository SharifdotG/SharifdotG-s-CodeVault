#include <bits/stdc++.h>

using namespace std;

int main() {
    cout << "Can sample output exist without sample input? If a tree falls in a forest and no one is around to hear it, does it make a sound? Is the previous question even relevant?" << endl;
    
    return 0;
}