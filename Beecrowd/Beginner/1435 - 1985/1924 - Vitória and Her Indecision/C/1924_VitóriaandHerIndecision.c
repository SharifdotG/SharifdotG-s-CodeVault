#include <stdio.h>

int main() {
    int courses;
    scanf("%d", &courses);

    while (courses--) {
        char course[50];
        scanf("%s", course);
    }

    printf("Ciencia da Computacao\n");

    return 0;
}
