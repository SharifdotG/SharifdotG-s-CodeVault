#include <stdio.h>

int main() {
    int a, b, c, d;
    scanf("%d %d %d %d", &a, &b, &c, &d);

    int difference = a * b - c * d;

    printf("DIFERENCA = %d\n", difference);

    return 0;
}
