#include <bits/stdc++.h>

using namespace std;

int main() {
    int radius;
    cin >> radius;

    cout << fixed << setprecision(6) << M_PI * radius * radius << endl;
    cout << fixed << setprecision(6) << 2 * radius * radius << endl;

    return 0;
}
