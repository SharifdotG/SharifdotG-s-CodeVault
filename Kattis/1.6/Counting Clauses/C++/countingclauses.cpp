#include <bits/stdc++.h>

using namespace std;

int main() {
    int m, n;
    cin >> m >> n;

    for (int i = 0; i < n; i++) {
        int a, b, c;
        cin >> a >> b >> c;
    }

    if (m < 8) {
        cout << "unsatisfactory" << endl;
    } else {
        cout << "satisfactory" << endl;
    }

    return 0;
}
