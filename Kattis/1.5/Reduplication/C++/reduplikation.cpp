#include <bits/stdc++.h>

using namespace std;

int main() {
    string word;
    int timesToRepeat;
    
    cin >> word >> timesToRepeat;
    
    for (int i = 0; i < timesToRepeat; ++i) {
        cout << word;
    }

    return 0;
}
